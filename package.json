{
  "name": "unmangle-chunk-webpack-plugin",
  "version": "1.0.6",
  "description": "This plugin aims to avoid manipulation of specific modules during the Webpack (Terser) compilation process.",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "rm -rf build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "peerDependencies": {
    "terser-webpack-plugin": "^5.3.10"
  },
  "author": "Simone De Vittorio",
  "license": "MIT"
}
